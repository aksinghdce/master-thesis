capture parameters:

series 1, 2 and 3, 1 st script from each series
10 repetitions per script,

display 0%, screen locking disabled
device fully charged

prefetch 10 pages, next 2 when missing
no on disk cache
with gzip
